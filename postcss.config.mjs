const config = {
  plugins: {
    "@tailwindcss/postcss": {
      css: {
        plugins: {
          typography: true // Enable the typography plugin here
        },
      },
    },
  },
};
export default config;